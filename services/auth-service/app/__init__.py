# Auth Service Application
# It tells Python "this folder is a package" so you can import from it. For example:
# from app.database import get_db
# Without __init__.py, that import fails. It can be completely empty — just needs to exist.